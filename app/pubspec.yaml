name: mogicians_manual
description: An open-source Flutter implementation of Mogician's Manual.
publish_to: 'none'

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
version: 10.1.2+100102

environment:
  sdk: ^3.13.0
  flutter: ">=3.47.0"

dependencies:
  flutter:
    sdk: flutter
  scoped_model: ^2.0.0
  fluttertoast: ^10.0.0
  flutter_staggered_grid_view: ^0.7.0
  share_plus: ^13.3.0
  audioplayers: ^6.8.1
  audio_service: ^0.18.19
  audio_session: ^0.2.4
  url_launcher: ^6.3.2
  photo_view: ^0.15.0
  pdfx: ^2.11.0
  # Pixel-level obfuscation of shared memes (pure Dart).
  image: ^4.9.2
  meta: ^1.19.0

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  # Lets the widget test swap in a fake audio platform.
  audioplayers_platform_interface: ^7.2.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/audio/
    - assets/images/
    - assets/data/
    - assets/pdf/

  fonts:
    - family: MaterialDesignIcons
      fonts:
        - asset: assets/fonts/MaterialDesignIcons-subset.ttf
