<?xml version="1.0" encoding="utf-8"?>
<!-- These drawables are referenced only by name at runtime (the notification's
     small icon from AudioServiceConfig, audio_service's play/pause/stop action
     icons), so release resource shrinking must not drop them. -->
<resources xmlns:tools="http://schemas.android.com/tools"
    tools:keep="@drawable/ic_stat_music,@drawable/audio_service_*" />
